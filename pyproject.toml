[project]
name = "penn"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "ipykernel>=7.3.0",
    "jupyter>=1.1.1",
    "numpy>=2.5.2",
    "pandas>=3.0.5",
    "rdkit>=2026.3.5",
    "scikit-learn>=1.9.0",
    "torch>=2.13.0",
    "wandb>=0.28.2",
]

[tool.pyright]
extraPaths = ["."]
executionEnvironments = [
  { root = "." }
]
